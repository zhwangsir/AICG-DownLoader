# ComfyUI 模型下载器 — 开发提示词 / 项目上下文

> 把本文件完整粘贴给 AI 编程助手（Claude Code / Cursor 等），即可获得继续开发本项目所需的全部上下文。
> 角色设定：你是一名资深 Rust 工程师，负责维护和迭代下面这个跨平台桌面应用。

---

## 1. 项目目标

为本地 ComfyUI 用户做一个**跨平台原生桌面下载器**，让用户不碰命令行就能把图像/视频模型下载到 ComfyUI 对应目录。核心痛点：模型分散在 Civitai 和 HuggingFace，文件大、需断点续传、要放进正确子目录、国内需走镜像。

目标平台：Windows / macOS / Linux。开发者主力机：Windows 11 + RTX 5080 (16GB 显存)，ComfyUI 安装在 `D:\ComfyUI`。

## 2. 技术栈（已锁定，勿随意更换）

- 语言：Rust (edition 2021，工具链 stable ≥ 1.96)
- GUI：`eframe` + `egui` 0.29（原生即时模式 GUI，单文件可执行）
- 图片加载：`egui_extras` 0.29（features: http, file, image）+ `image` 0.25（jpeg/png/webp）
- 网络：`ureq` 2（`default-features=false, features=["tls"]`，rustls，**禁用 gzip** 避免 Content-Length 与断点续传冲突）
- 序列化：`serde` + `serde_json`（Civitai/HF 响应用 `serde_json::Value` 宽松解析，抗 schema 变动）
- 正则：`regex` 1（解析链接）
- 哈希：`sha2` 0.10（SHA256 校验，自动用 SHA-NI 硬件加速）
- 文件对话框：`rfd` 0.15（原生目录/文件选择）
- 哈希：`sha2` 0.10（下载 SHA256 校验，纯 Rust，自动用 SHA-NI 硬件加速）

## 3. 目录结构

```
下载器-rust/
├── Cargo.toml          依赖清单
├── src/main.rs         全部源码（约 2600 行，单文件，含测试模块）
├── config.json         运行配置（便携模式：exe 同目录存在则优先用；否则用系统配置目录 %APPDATA%\comfy-downloader / ~/.config/comfy-downloader）
├── README.md           编译/使用说明
├── 编译并运行.bat       Windows 一键编译启动（见第 8 节坑）
└── 开发提示词.md        本文件
```

构建产物：`target/release/comfy-downloader(.exe)`。

## 4. 已实现功能（当前状态：2026-06-11 已在主力机完成首次编译运行验证，`cargo clippy` 零警告；窗口/中文渲染/Civitai API/hf-mirror Range 续传链路均实测通过）

- **🔍 搜索 Civitai**：关键词 + 类型筛选调 `GET /api/v1/models`，结果卡片展示预览图、类型、底模、下载量；点"下载"二次解析后弹确认框。
- **🔗 链接解析**：粘贴 Civitai 模型页（`civitai.com|red|work/models/{id}?modelVersionId=`）或 HuggingFace 文件页（`.../resolve|blob/{branch}/{path}`），自动识别类型、文件名、目标目录。
- **🎬 作品页一键成套**（2026-06-11 新增，同日改用 __NEXT_DATA__ 修复视频页）：粘贴 Civitai 图片/视频/帖子链接（`/images|posts/{id}`）→ 爬取页面 `__NEXT_DATA__` JSON 提取资源 → 批量解析为资源清单勾选弹窗（`pending_set`）→ 整套入队（精确版本 + SHA256）。每个资源带模型简介（`Resolved.desc`，从 `/api/v1/models/{id}` 的 description 剥 HTML 截断）。提取逻辑见第 6 节；真实作品页 e2e 已覆盖。创作者隐藏生成信息的作品会明确报错。
- **📦 预设套餐**：内置「Wan 2.2 I2V 视频全套（16G 显存适配）」和「Flux.1 fp8 文生图」，一键把整套文件入队。
- **🗂️ 模型库扫描**：遍历 ComfyUI 各 `models/*` 子目录列出已有模型文件。
- **⚙️ 设置**：ComfyUI 根目录、Civitai 密钥（脱敏显示）、hf-mirror 镜像开关、并发数（1-4），保存到 `config.json`（便携/系统目录双模式，页面底部显示实际路径；保存失败会如实报错）。
- **下载队列**：底部面板，实时进度条/速度，**断点续传**（`.part` 临时文件 + HTTP Range），可取消，已存在文件自动跳过。
- **中文显示**：启动时从系统字体目录自动加载 CJK 字体（Win 微软雅黑/黑体、mac 苹方、Linux Noto/文泉驿）；候选路径未命中时递归扫描字体目录兜底（覆盖 Arch/Fedora），仍失败则顶部显示英文警告。
- **SHA256 校验**：Civitai 哈希取自 API `files[].hashes.SHA256`（严格对应所选版本）；HF 经 tree API 查 `lfs.oid`（套餐/恢复任务在下载线程懒查）。边下边算（续传先把已有 .part 在发请求**之前**喂进哈希器），不匹配删文件报错，匹配任务行显示绿色 `SHA256 ✓`。
- **任务持久化**：排队中/下载中/失败 任务写 `tasks.json`（config 同目录），集合变化时落盘 + 退出兜底；启动自动恢复入队（失败任务自动重试），靠 .part 续传。单实例文件锁（`File::try_lock`）：第二个实例不恢复不写盘并顶部警告，防两进程并发写同一 .part。终态任务可「移除」（同时从 tasks.json 清掉）。
- **📋 工作流分析**：拖拽 .json 进窗口 / 粘贴路径 / 粘贴 JSON；同时支持界面格式（递归含子图 definitions.subgraphs）与 API 格式；节点类型→目录映射（unet↔diffusion_models、text_encoders↔clip 别名互查），一次性建全库文件名索引判存在；缺失项一键跳 Civitai 搜索。Note 节点 URL/长文本/多行被过滤。
- **系统代理**：`agent()` 读 `HTTPS_PROXY`/`HTTP_PROXY` 环境变量（ureq 默认不读）——Clash 仅系统代理模式、或节点过滤 rustls JA3 指纹导致直连 TLS 失败时仍可联网。
- **测试**：`cargo test --release` 跑单元测试（12 个）；`cargo test --release -- --ignored` 跑真实网络 e2e（下载/续传/416 自愈/SHA256 通过与失败路径，写临时目录不碰 ComfyUI）。

## 5. 代码架构（src/main.rs 单文件，按区块组织）

- **配置区**：`Config` 结构（serde）、`load_config`/`save_config`（读写 exe 同目录 config.json）、`type_dir()`（模型类型→子目录映射）、`guess_type()`（按文件名猜类型）、`hf_base()`（镜像切换）。
- **数据结构**：`SearchItem` / `VerInfo` / `Resolved`（解析结果，含多版本列表）/ `Task`（下载任务，`TaskRef = Arc<Mutex<Task>>`）。
- **网络函数**（独立函数，供后台线程调用，不依赖 &self）：`agent()`、`civitai_search()`、`resolve_url()`。
- **下载引擎**：全局 `static ACTIVE: AtomicUsize` 作并发闸门 + `static NEXT_ID`；`start_task()` 创建任务入队并起线程；`download_file()` 流式下载、Range 续传、分块更新进度。
- **模型库**：`scan_library()`。
- **预设**：`presets()` 返回 (key, 标题, [(url, 文件名, 子目录)])，`hf_file()` 辅助构造。
- **主题**：文件顶部 `C_*` 调色板常量 + `setup_style()`（深色主题、强调蓝 #6091F0、圆角、字号、间距）；`chip()`/`status_chip()` 彩色徽章、`accent_btn()` 主操作按钮（每页一个蓝色主操作）。颜色语义固定：蓝=进行/主操作、绿=成功、红=失败、黄=警告，进度条颜色随任务状态。空队列时底部面板收成一行（`exact_height(40)`）；搜索空态有示例关键词引导；错误行单行截断+悬停看全文；失败任务有「重试」按钮。`COMFY_START_TAB` 环境变量可指定启动页（截图/调试用）。
- **GUI**：`App` 持有所有 UI 状态 + `Sender/Receiver<Msg>`（mpsc）+ `downloads: Arc<Mutex<Vec<TaskRef>>>`。`impl eframe::App for App::update()` 每帧：先 `try_recv` 处理后台结果 → 顶部标签栏 → 底部下载队列面板 → 中央按 `Tab` 枚举分发到 `ui_search/ui_link/ui_preset/ui_library/ui_settings` → `ui_pending` 弹窗 → `request_repaint_after(500ms)`。
- **字体**：`install_cjk_font()`。
- **入口**：`main()` 设置 `NativeOptions` 并 `eframe::run_native`。

### 并发模型
UI 线程永不阻塞网络：搜索/解析/扫描各起一次性线程，结果经 mpsc 回传；下载各起常驻线程，靠 `ACTIVE` 原子量限并发，进度直接写 `Arc<Mutex<Task>>`，UI 每帧读锁渲染。

## 6. 外部接口要点

- Civitai 搜索：`https://civitai.com/api/v1/models?query=&types=&limit=24&nsfw=true&sort=Most%20Downloaded`，带 `Authorization: Bearer <token>`。
- Civitai 下载：`https://civitai.com/api/download/models/{versionId}`，带 token。文件名可从响应 `Content-Disposition` 兜底。
- HuggingFace：`{base}/{repo}/resolve/{branch}/{path}`，`base` 在 `huggingface.co` 与 `hf-mirror.com`（国内）间切换。
- **Civitai 作品页（图片/视频/帖子）资源解析**：内部 TRPC 接口已对外封锁（401 明示用公开 API），公开 API 的 images 接口完全不返回 video、meta 大多为空。可行方案是**抓作品页 HTML 里的 `__NEXT_DATA__` 内嵌 JSON**（服务端渲染，无 Cloudflare 拦截，任意 UA 可取，对图片/视频/帖子都有）：
  - 图片/视频页（同走 `/images/{id}` 路由，结构完全一致）：`...resources[]`，每项含 `modelId`+`modelVersionId`+`modelName`+`modelType`+`baseModel`。
  - 帖子页 `/posts/{id}`：只有裸 `modelVersionIds[]`/`modelVersionIdsManual[]`，按 versionId 经 `/api/v1/model-versions/{id}` 解析。
  - 实现 `extract_media_resources`（递归 walk JSON 收集，去重）→ `resolve_media_page`（逐个走公开 API）。**不要回退到只抓渲染后的 `<ul>`**——视频页那个 `<ul>` 是客户端渲染的，旧版只抓 `<ul>` 导致视频页报"未找到 Resources used"。`<ul>` 仅作 JSON 解析失败时的兜底。
- HF 文件哈希：`GET {base}/api/models/{repo}/tree/{branch}/{dir}` 取条目的 `lfs.oid`（即 SHA256）。**必须用 GET**：hf-mirror 对 API 返回 308 重定向，ureq 只对 GET/HEAD 跟随重定向，POST 的 paths-info 会拿到空 308 响应。单页 1000 项截断时查不到则跳过校验。
- 响应解析一律走 `serde_json::Value` + `.get().and_then()`，缺字段给默认值，不要用强类型 struct 反序列化（Civitai schema 常变）。

## 7. 编译与运行

```bash
cargo build --release
# 产物: target/release/comfy-downloader(.exe)
```
首次编译下载约 500 个依赖 crate，需几分钟。Windows 需要 MSVC C++ Build Tools 提供 `link.exe`（装了 Visual Studio 2022 即可）。`config.json` 需与可执行文件放同目录。

## 8. 关键约束与已踩的坑（务必遵守，否则会重蹈覆辙）

1. **Windows .bat 必须 CRLF 换行 + 纯 ASCII（或 GBK）**。曾用 UTF-8/LF 写 bat，cmd.exe 解析错乱：吃掉字符（`cargo`→`argo.exe`）、把中文注释当命令执行。结论：bat 一律英文 + CRLF，无 BOM。
2. **cargo 找不到（PATH 未刷新）**：刚装 Rust 后旧进程/旧 cmd 不带新 PATH。脚本应回退到 `%USERPROFILE%\.cargo\bin\cargo.exe`，或提示用户重启。
3. **CJK 字体**：egui 默认无中文字形，必须运行时加载系统字体，否则中文显示为方块。
4. **ureq 禁用 gzip**：开 gzip 会让 Content-Length 与实际写入字节不符，破坏断点续传与进度条。
5. **egui/eframe/egui_extras 三者版本必须一致**（同为 0.29.x），否则类型不兼容。
6. **egui 通过 `eframe::egui` 引入**（项目未直接依赖 egui crate），文件顶部 `use eframe::egui;`。
7. **预览图 URL** 来自 Civitai CDN，可能含 NSFW；这是用户本机工具，按原样显示，不做内容过滤（但合规：仅限合法用途）。
8. 沙箱/挂载注意（仅对自动化环境）：文件工具写入与某些 Linux 挂载存在同步延迟，验证以实际磁盘为准。
9. **ureq 不读系统代理环境变量**，且部分 Clash 节点会按 TLS 指纹（JA3）过滤 rustls 的握手（curl/schannel 正常而 rust 程序 TLS init 失败，报 `unexpected end of file`）。`agent()` 已显式读 `HTTPS_PROXY`/`HTTP_PROXY` 走本地代理端口，勿删。
10. **长时间不消费 HTTP 响应 body 会被服务端断连**（nginx send_timeout 默认 60s）。任何"拿到响应后先做耗时本地工作（如预哈希大 .part）再读 body"的写法都会让大文件操作必败——耗时工作必须放在发请求之前。
11. **config.json/tasks.json 读入时要容忍 UTF-8 BOM**（`trim_start_matches('\u{feff}')`）：用户用记事本手改、或外部工具（PowerShell `Out-File -Encoding utf8`）写入都会带 BOM，serde 解析失败会静默回退默认配置，极难排查。

## 9. 路线图 / 待办（按优先级，挑选实现时先与我确认）

- [x] **本机首次编译验证**（2026-06-11 完成）：`cargo build --release` 通过，窗口/中文渲染截图确认，Civitai 搜索 API、下载 307 重定向、hf-mirror Range 206 续传链路实测通过。同日修复多代理审查确认的缺陷：续传 200/206 校验（防静默损坏）、ureq 读超时 30s（防任务僵死锁队列）、416 自愈、同名任务去重入队、版本切换同步文件名/大小、搜索页错误提示+中文化、预览图过滤 video 类型、`~` 展开、文件名清洗（非法字符/百分号解码）、保存设置失败反馈、并发闸门 compare_exchange、emoji 缺字形（🧩🗂️⚙️→去除/📁/⚙）。交互级实测（实际点击搜索/下载/断点续传）待用户操作确认。
- [x] **遗留决策落地**（2026-06-11，同日第二轮）：config.json 便携+系统目录双模式（设置页显示实际路径）；Linux 字体路径扩充+目录扫描兜底+缺字体英文警告（内嵌字体留给打包阶段）；新增测试模块——4 个单元测试 + `download_resume_e2e` 真实网络端到端测试（全量/跳过/续传/416 自愈四场景，已实跑通过）。`windows_subsystem` 属性已排除 test 构建以保测试输出可见。第二轮对抗性审查唯一确认项已修复：字体加载加 sfnt magic 校验（00010000/OTTO/ttcf/true），损坏字体文件不再导致 epaint 首帧 panic 启动闪退，扫描遇无效文件继续找下一个。
- [x] **SHA256 校验**（2026-06-11 完成）：边下边算、续传预哈希在发请求前完成、版本严格对应、校验失败删文件、通过显示绿色徽标；e2e 覆盖通过/失败/续传三路径。
- [x] **任务持久化**（2026-06-11 完成）：tasks.json + 失败任务保留重试 + 单实例文件锁 + 终态任务移除按钮。
- [x] **工作流缺失模型分析**（2026-06-11 完成）：双格式+子图解析、全库索引查存在、拖拽/路径/JSON 三输入、缺失项跳搜索。注：真正"自动批量下载缺失模型"需要文件名→下载源的数据库，目前是跳转 Civitai 搜索，如需更强可后续讨论。
  三项均经 16 代理对抗性审查（23 发现/10 确认全部修复，含：预哈希时机回归、失败任务丢持久化、多实例并发写 .part、Note 节点 URL 误报、UI 线程扫盘卡顿等）。同时新增 agent() 系统代理支持（第 8 节坑 9）。
- [x] **Civitai 搜索分页 + 底模过滤**（2026-06-11 完成）：metadata.nextPage 游标分页「加载更多」、baseModels 参数过滤（SD1.5/SDXL/Pony/Illustrious/Flux/Wan 等），e2e 验证过滤与翻页去重。
- [x] **全局暂停/恢复 + 失败自动重试**（2026-06-11 完成）：static PAUSED 原子量，下载中任务在分块边界退出为「已暂停」保留 .part，恢复时按元数据重新入队续传；排队任务原地等待。自动重试指数退避 2/4/8s，401/403/404/SHA256 失败不重试，「重试等待」状态可取消且会持久化。限速按用户决定不做。
- [x] **rfd 可视化选择**（2026-06-11 完成）：设置页「浏览…」选 ComfyUI 根目录、工作流页「选择文件…」选 json。rfd 0.15。
- [ ] Linux/macOS 下的字体路径与打包（AppImage / .app）验证。

## 10. 给 AI 助手的工作要求

- 改动前先读 `src/main.rs` 全文，保持单文件结构与现有命名风格。
- 任何涉及网络响应解析的改动，先确认真实 JSON 形状（schema 易变），用 Value 宽松解析。
- 新增依赖要说明理由并保持轻量；优先纯 Rust、跨平台、无重型系统依赖。
- 每次改完跑 `cargo check`（理想是 `cargo clippy`）确保零错误，再交付。
- 涉及 Windows 脚本/路径/编码时，主动套用第 8 节的坑，别让我再踩一遍。
- 中文交流；代码注释中文可以，但**生成的 .bat/.cmd 只用 ASCII 英文**。
```
